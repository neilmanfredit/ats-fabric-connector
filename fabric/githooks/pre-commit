#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

./.githooks/pre-commit-hook.sh
fabric/scripts/check-sensitive-files.sh staged
fabric/scripts/check-structure.sh
